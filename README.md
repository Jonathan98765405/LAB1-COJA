# LAB1-COJA

1. create folder in local storage named Lab1_Coja
2. on terminal change the directory into my folder
3. configure user and email
4. use git init command
5. use git add . to track changes on main
6. create no-style branch using git branch no-style
7. use git add .
8. then on html file on no-style branch created no style portfolio
9. then on main branch, created with style portfolio

Git Commands Used
git config --global user.name
git config --global user.email
git init
git add .
git status
git commit -m
git branch
git switch -branchname
git remote add origin https://github.com/Jonathan98765405/LAB1-COJA.git
git remote -v
git push -u origin main

